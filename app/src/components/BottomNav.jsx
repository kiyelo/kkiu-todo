import {CircleIcon,HomeIcon,MoreIcon} from './Icons.jsx'
const items=[['home','끼우',HomeIcon],['circle','끼리',CircleIcon],['more','더보기',MoreIcon]]
export default function BottomNav({tab,unread,onChange}){const idx=Math.max(0,items.findIndex(x=>x[0]===tab));return <nav className="nav" id="nav"><div className="nav-ind" style={{left:'8px',width:'calc((100% - 16px)/3)',transform:`translateX(${idx*100}%)`}}/>{items.map(([id,label,Icon])=><button key={id} className={`nb${tab===id?' on':''}`} onClick={()=>onChange(id)}><Icon/>{id==='circle'&&unread>0&&<i className="nbadge">{unread>99?'99+':unread}</i>}<span>{label}</span></button>)}</nav>}
