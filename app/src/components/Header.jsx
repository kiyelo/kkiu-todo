import { CheckIcon, MoreIcon, SearchIcon } from './Icons.jsx'
export default function Header({tab,circle,searchOpen,onSearch,onCircleSelect,onCompleted,onManage}){
 if(tab==='home')return <div className="head home-head"><div className="headrow"><h1 className="ttl">할 일</h1><div className="hicons"><button className="hbtn" onClick={onSearch} aria-label={searchOpen?'검색 닫기':'검색'}>{searchOpen?'×':<SearchIcon/>}</button>{!searchOpen&&<button className="hbtn" onClick={onCompleted} aria-label="완료 목록"><CheckIcon/></button>}</div></div></div>
 if(tab==='circle')return <div className="head circle-head"><div className="headrow">{circle?<button className="ttlbtn" onClick={onCircleSelect}><span className="circle-emoji">{circle.emoji}</span><span className="circle-title-window"><span className={`circle-title-text${circle.name.length>10?' stream':''}`}>{circle.name}</span></span><span className="car">⌄</span>{circle.unread>0&&<i className="alert-dot"/>}</button>:<h1 className="ttl">끼리</h1>}<div className="circle-tools">{circle&&<p className="head-sub">멤버 {circle.members?.length||1}명</p>}<div className="hicons"><button className="hbtn" onClick={onSearch} aria-label={searchOpen?'검색 닫기':'검색'}>{searchOpen?'×':<SearchIcon/>}</button>{!searchOpen&&<button className="hbtn" onClick={onCompleted} aria-label="완료 목록"><CheckIcon/></button>}{circle&&<button className="hbtn" onClick={onManage} aria-label="끼리 관리"><MoreIcon/></button>}</div></div></div></div>
 return <div className="head more-head"><div className="headrow"><h1 className="ttl">더보기</h1><div className="hicons"/></div></div>
}
